<li>
	{{drink_name}}

	<button class="change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}YUM!{{else}}EAT ME!{{/if}}
	</button>

	<button class="delete-drink" data-id="{{id}}">Delete</button>
</li>
